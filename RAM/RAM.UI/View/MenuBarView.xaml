<UserControl
	x:Class="RAM.UI.View.MenuBarView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewModel="clr-namespace:RAM.Infrastructure.ViewModel;assembly=RAM.Infrastructure"
	DataContext="{Binding MenuBarViewModel, Source={StaticResource ModelViewLocator}}"
	mc:Ignorable="d">

    <Menu ItemsSource="{Binding MenuItemViewModels}">
		<Menu.ItemContainerStyle>
			<Style TargetType="{x:Type MenuItem}">
				<Setter Property="Command" Value="{Binding Command}" />
			</Style>
		</Menu.ItemContainerStyle>
		<Menu.ItemTemplate>
			<HierarchicalDataTemplate DataType="{x:Type viewModel:MenuItemViewModel}" ItemsSource="{Binding Path=Children}">
				<TextBlock Text="{Binding Model.Header}" />
			</HierarchicalDataTemplate>
		</Menu.ItemTemplate>
	</Menu>
</UserControl>
